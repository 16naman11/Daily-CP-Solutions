#include <stdio.h>

int main() {
   int a;
    printf("enter value of a : ");
    scanf("%c", &a);
    if(a>=48&&a<=57){
        printf("the key pressed is a digit ");
        
    }
    else
    printf("the key pressed is not a digit");
    return 0;
    
   
}
